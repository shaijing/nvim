return {
    'glepnir/dashboard-nvim',
    cond = false,
    event = 'VimEnter',
    config = function()
        require('dashboard').setup(require("plugins.config").dashboard)
    end,
    dependencies = { { 'nvim-tree/nvim-web-devicons' } }

}
